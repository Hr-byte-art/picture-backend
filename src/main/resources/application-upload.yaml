# 对象存储配置
cos:
  client:
    host: *
    secret-id: *
    secret-key: *
    region: *
    bucket: *
aliYunAi:
  apiKey: *
spring:
  #    配置数据库
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/picture?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: *
  #    redis配置
  redis:
    host: localhost
    port: 6379
    password: *
    database: 4
    timeout: 5000